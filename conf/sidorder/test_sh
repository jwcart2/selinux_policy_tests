
PREFIX=/home/jwcart2/local

export LD_LIBRARY_PATH=${PREFIX}/lib:${PREFIX}/usr/lib:${PREFIX}/usr/lib64:${PREFIX}/usr/libexec:${LD_LIBRARY_PATH}


for T in 01
do
	echo "checkpolicy: test_$T.bin"
	$PREFIX/usr/bin/checkpolicy -M -o test_$T.bin test_$T.conf

	echo "checkpolicy -F: test_$T.bin.conf"
	$PREFIX/usr/bin/checkpolicy -M -b -F -o test_$T.bin.conf test_$T.bin

done

mv *.bin tmp/
mv *.bin.conf tmp/
